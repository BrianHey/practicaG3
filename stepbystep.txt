git init = Inicio del repositorio

git add . = Agrega los cambios al stage

git commit . = Agrega los cambios al empaquetado

git push <repositorio destino> <rama destino> = Envía el commit al destino

git clone <repositorio remoto> = Clona un repositorio remoto trayendo el .git con su historial

--------
Otros:

git status => ver los archivos en stage
git log => ver los commits realizados